package main

import "fmt"

func main() {
	var n, m int // количество вершин и ребер в графе
	fmt.Scan(&n, &m)
	graph := make([]int, m)
	for i := 0; i < m; i++ {
		var a, b int
		fmt.Scan(&a, &b)

	}
}
